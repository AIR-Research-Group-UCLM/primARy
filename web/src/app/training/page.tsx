import Typography from "@mui/material/Typography"

export default function Page() {
    return (
        <Typography paragraph>
            This is the page for model training
        </Typography>
    )
}